#include <iostream>

/* int main()
{
    // You can run your own test code here as you develop your class to check that it works as you expected.
    // Compile `sequence_test.cpp` and `sequence_exam.cpp` as separate executables to test your class with that code.
    // They both contain a main function, meaning you cannot compile them with this `main.cpp` file.
    std::cout << "Hello, World!" << std::endl;
    return EXIT_SUCCESS;
} */